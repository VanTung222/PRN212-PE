<!-- TranVanTungWPF/Views/LoginWindow.xaml -->
<Window x:Class="TranVanTungWPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="Email" Margin="0,20,0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="3" Text="Password" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,0,0,10"/>
        <Button Grid.Row="4" Content="Login" Width="100" HorizontalAlignment="Left" Margin="122,62,0,-32" Command="{Binding LoginCommand}" CommandParameter="{Binding Text, ElementName=PasswordBox}"/>
        <TextBlock Grid.Row="5" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>