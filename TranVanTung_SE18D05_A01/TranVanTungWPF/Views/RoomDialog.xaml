<!-- TranVanTungWPF/Views/RoomDialog.xaml -->
<Window x:Class="TranVanTungWPF.Views.RoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Dialog" Height="400" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="Room Number" Margin="0,10,0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Room.RoomNumber, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="3" Text="Description" Margin="0,10,0,5"/>
        <TextBox Grid.Row="4" Text="{Binding Room.RoomDescription, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="5" Text="Max Capacity" Margin="0,10,0,5"/>
        <TextBox Grid.Row="6" Text="{Binding Room.RoomMaxCapacity, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="7" Text="Price/Day" Margin="0,10,0,5"/>
        <TextBox Grid.Row="8" Text="{Binding Room.RoomPricePerDate, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="9" Text="Room Type" Margin="0,10,0,5"/>
        <ComboBox Grid.Row="10" ItemsSource="{Binding RoomTypes}" DisplayMemberPath="RoomTypeName" SelectedValuePath="RoomTypeID" SelectedValue="{Binding Room.RoomTypeID}"/>
        <StackPanel Grid.Row="11" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Save" Width="100" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="100" Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
        </StackPanel>
        <TextBlock Grid.Row="12" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>
    </Grid>
</Window>